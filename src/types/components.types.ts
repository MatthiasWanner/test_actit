import {
  TailwindColor,
  TailwindFixedSize,
  TailwindNuance,
} from './tailwind.types';

export interface IBubbleProps {
  color: TailwindColor;
  nuance: TailwindNuance;
  size: TailwindFixedSize;
}
